@{
  Layout = "_Layout";
}
@using Garden.Models;
@model List<Garden.Models.Plot>

<div class="behind">
@if(@Model.Count() == 0)
{
  <h3>You have no plots!</h3>
}
else
{
  <h2>Plots</h2>
  @foreach(Plot plot in Model)
  {
    <p>@Html.ActionLink($"{plot.Name}", "Details", new { id = plot.PlotId }, new { @class = "ButtonText" })</p>
  }
}
<!-- <h2>Here's your Plot: </h2>


<div class="plot">
  <section id="top-bar">
    <div id="clone-container">
      <div id="scroll-box">
        <div id="tile-container">

          <div class="tile-wrapper">
            <div draggable= "true" id="tile" class = "tile">Tomatoes</div>
          </div>
          <div class="tile-wrapper">
            <div draggable= "true" id="tile2" class ="tile">Cabbage</div>
          </div>
          <div class="tile-wrapper">
            <div draggable= "true" id="tile3" class ="tile">Corn</div>
          </div>
          <div class="tile-wrapper">
            <div draggable= "true" id="tile4" class ="tile">Zucchini</div>
          </div>
          <div class="tile-wrapper">
            <div draggable= "true" id="tile5" class ="tile">Cucumber</div>
          </div>
          <div class="tile-wrapper"> 
            <div draggable= "true" id="tile6" class ="tile">Pumpkin</div>
          </div>

        </div>
      </div>
    </div>
  </section>
  <section class="content"></section>
  <div id="drop-panel">
    <h3>DRAG ITEMS HERE</h3>
  </div>
</div> -->


<p class="btn btn-light">@Html.ActionLink("Add Plot", "Create")</p>
<!-- <p class="btn btn-light">@Html.ActionLink("See Plots", "Details")</p> -->
</div>