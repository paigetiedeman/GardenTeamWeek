@{
  Layout = "_Layout";
}

@model Garden.Models.Plot

<div class="behind">
<h2>@Html.DisplayFor(model => model.Name)</h2>

<h2>Here's your Plot: </h2>
</div>
<div class="plot">
  <section id="top-bar">
    <div id="clone-container">
      <div id="scroll-box" ondragover="allowDrop(event)" ondrop="returnSeed(event)">
        <div id="tile-container">

          <div class="tile-wrapper">
            <div draggable= "true" id="tile" class = "tile" ondragstart="drag(event)">Tomato<br>🍅</div>
          </div>
          <!-- 🍅🍅 -->
          <div class="tile-wrapper">
            <div draggable= "true" id="tile2" class ="tile" ondragstart="drag(event)">Cabbage<br>🥬</div>
          </div>
          <!-- 🥬🥬 -->
          <div class="tile-wrapper">
            <div draggable= "true" id="tile7" class ="tile" ondragstart="drag(event)">Banana<br>🍌</div>
          </div>
          <!-- 🍌🍌 -->
          <div class="tile-wrapper">
            <div draggable= "true" id="tile3" class ="tile" ondragstart="drag(event)">Corn<br>🌽</div>
          </div>
          <!-- 🌽🌽 -->
          <div class="tile-wrapper">
            <div draggable= "true" id="tile4" class ="tile" ondragstart="drag(event)">Strawberry<br> 🍓</div>
          </div>
          <!-- 🍓🍓 -->
          <div class="tile-wrapper">
            <div draggable= "true" id="tile5" class ="tile" ondragstart="drag(event)">Cucumber<br> 🥒</div>
          </div>
          <!-- 🥒🥒🥒 -->
          <div class="tile-wrapper"> 
            <div draggable= "true" id="tile6" class ="tile" ondragstart="drag(event)">Pumpkin<br> 🎃</div>
          </div>
          <!-- 🎃🎃 -->
        </div>
      </div>
    </div>
  </section>
  <section class="content"></section>
  <div id="drop-panel">
    <h3>DRAG ITEMS HERE</h3>
    <h4>@Model.Width X @Model.Length X @Model.Depth </h4> 

    @{ int sqFeet = @Model.Width * @Model.Length; }
    @for(int i = 1; i <= sqFeet; i++)
    {
      <div id="div@i" ondrop="drop(event)" ondragover="allowDrop(event)"></div>
    }
  </div>
</div>


<div class="behind">
<h4>Sun Needed: @Model.Sun</h4>
<h5><em>Soil Type: @Model.Soil</em></h5>
<!-- Where we connect to api and transmit zipcode to hardiness zone -->
<h4>Zipcode: @Model.ZipCode</h4>
<h4><a class="HardText" href="/Home/LearningZone">Hardiness</a> Zone: @ViewBag.thisZipZone.Zone</h4>
<h4>Plot Size: @Model.Width X @Model.Length X @Model.Depth</h4>



<p class="btn btn-light">@Html.ActionLink("Edit this Plot", "Edit", new { id = Model.PlotId }, new { @class = "ButtonText" })</p>
<p class="btn btn-light">@Html.ActionLink("Delete this Plot", "Delete", new { id = Model.PlotId}, new { @class = "ButtonText"})</p>
</div>
